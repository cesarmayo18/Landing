
.commerce-form
  %h2.title-form
    Queremos ayudarte a
    = succeed " más" do
      %span vender
  = form_for @commerce do |f|
    - if @commerce.errors.any?
      #error_explanation
        %ul
          - @commerce.errors.full_messages.each do |message|
            %li= message

    .field
      = f.label :Nombre_del_comercio
      = f.text_field :commerce_name, required: true
    .field
      = f.label :Sitio_web
      = f.text_field :website, required: true
    .field
      = f.label :Nombre_de_contacto, required: true
      = f.text_field :name
    .field
      = f.label :Correo
      = f.text_field :email, required: true
    .actions
      = f.submit 'Enviar', class:'btn'
